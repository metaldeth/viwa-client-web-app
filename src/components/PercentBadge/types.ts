/**
 * Свойства компонента PercentBadge
 */
export type PercentBadgeProps = {
  /**
   * Процент
   */
  percent?: number;
  /**
   * Наличие инверсии отображения процентов
   */
  hasInversion?: boolean;
};
